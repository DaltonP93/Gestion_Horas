node_modules/
*.log
__pycache__/
.next/
*.pyc
.venv/
venv/
dist/

# ── Secretos: NUNCA commitear ────────────────────────────────
.env
.env.*
!.env.example
!*/.env.example
# .env.production puede subirse SOLO si contiene URLs públicas (sin secretos)
# El archivo está validado: revisar antes de cada push.

# ── Capacitor — carpetas nativas generadas localmente ────────
web/android/
web/ios/
web/out/
web/.capacitor/

# ── Editores ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store

# ── Logs y temporales ────────────────────────────────────────
logs/
*.log.*
tmp/

# ── Artefactos de build de TypeScript ────────────────────────
# Caché incremental: lo regenera `tsc` / `next build` en cada corrida.
web/tsconfig.tsbuildinfo
*.tsbuildinfo

# ── Modelos de face-api ──────────────────────────────────────
# Se copian desde node_modules en cada build (web/scripts/sync-face-models.js),
# así el JS y los pesos son siempre de la misma versión exacta. No se versionan.
web/public/face-models/
